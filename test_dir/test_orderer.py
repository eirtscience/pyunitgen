
import unittest

		
class BatchSizeTest(unittest.TestCase):
		"""
		Tests for methods in the <_ast.ClassDef object at 0x7f2c1575f1d0> class.
		"""

		@classmethod
		def setUpClass(cls):
			pass #TODO

		@classmethod
		def tearDownClass(cls):
			pass #TODO

		def setUp(self):
			pass #TODO

		def tearDown(self):
			pass #TODO
	
		def test_dump(self):
				
      batchsize = BatchSize().dump() 
			
      self.assertIsNone(batchsize) 


class OrdererServerTest(unittest.TestCase):
		"""
		Tests for methods in the <_ast.ClassDef object at 0x7f2c156e1d30> class.
		"""

		@classmethod
		def setUpClass(cls):
			pass #TODO

		@classmethod
		def tearDownClass(cls):
			pass #TODO

		def setUp(self):
			pass #TODO

		def tearDown(self):
			pass #TODO
	


class OrdererTest(unittest.TestCase):
		"""
		Tests for methods in the <_ast.ClassDef object at 0x7f2c1578c198> class.
		"""

		@classmethod
		def setUpClass(cls):
			pass #TODO

		@classmethod
		def tearDownClass(cls):
			pass #TODO

		def setUp(self):
			pass #TODO

		def tearDown(self):
			pass #TODO
	
		def test_getDomain(self):
				
      orderer = Orderer().getDomain() 
			
      self.assertIsNone(orderer) 

		def test_add_chaincode(self):
				
      orderer = Orderer().add_chaincode() 
			
      self.assertIsNone(orderer) 

		def test_getChainCode(self):
				
      orderer = Orderer().getChainCode() 
			
      self.assertIsNone(orderer) 

		def test_getInitialChainCode(self):
				
      orderer = Orderer().getInitialChainCode() 
			
      self.assertIsNone(orderer) 

		def test_create_orderer(self):
				
      orderer = Orderer().create_orderer() 
			
      self.assertIsNone(orderer) 

		def test_getAllOrderer(self):
				
      orderer = Orderer().getAllOrderer() 
			
      self.assertIsNone(orderer) 

		def test_getlist_policies(self):
				
      orderer = Orderer().getlist_policies() 
			
      self.assertIsNone(orderer) 

		def test_getHostname(self):
				
      orderer = Orderer().getHostname() 
			
      self.assertIsNone(orderer) 

		def test_getHostport(self):
				
      orderer = Orderer().getHostport() 
			
      self.assertIsNone(orderer) 

		def test_getAnchorPeer(self):
				
      orderer = Orderer().getAnchorPeer() 
			
      self.assertIsNone(orderer) 

		def test_getOrdererMsp(self):
				
      orderer = Orderer().getOrdererMsp() 
			
      self.assertIsNone(orderer) 

		def test_dump(self):
				
      orderer = Orderer().dump() 
			
      self.assertIsNone(orderer) 

		def test_list_version(self):
				
      orderer = Orderer().list_version() 
			
      self.assertIsNone(orderer) 

		def test_dump_all_addresses(self):
				
      orderer = Orderer().dump_all_addresses() 
			
      self.assertIsNone(orderer) 

		def test_getAllOrdererName(self):
				
      orderer = Orderer().getAllOrdererName() 
			
      self.assertIsNone(orderer) 

		def test_create_consenter(self):
				
      orderer = Orderer().create_consenter() 
			
      self.assertIsNone(orderer) 

		def test_getBlockValidationPolicies(self):
				
      orderer = Orderer().getBlockValidationPolicies() 
			
      self.assertIsNone(orderer) 

		def test_dump_orderer(self):
				
      orderer = Orderer().dump_orderer() 
			
      self.assertIsNone(orderer) 
		